# 临时任务清单

更新时间：2026-05-29
状态：临时版，后续可持续追加

状态说明：
- `未开始`
- `进行中`
- `已完成`
- `暂缓`

## 按优先级排序的任务板

1. `[进行中]` 学习 `Git` 和 `GitHub` 的完整流程
2. `[已完成]` 学习 `Issue` 的使用方法
3. `[未开始]` 学习 `Codex` 的浏览器控制和屏幕控制
4. `[未开始]` 学习 `skill: frontend-design`、`Humanizer` 和其他好用的 skill
5. `[未开始]` 做一个自己的桌面待办清单应用
6. `[未开始]` 根据现有的 `Excel` 记账表格做一个记账系统
7. `[未开始]` 整理自己的知识并上传到知识库（当前已了解 `wiki` 知识库）
8. `[未开始]` 整理并保存 `Codex` 和 `Claude Code` 的安装流程、经验和踩坑记录
9. `[未开始]` 学习怎么通过 `Codex` 链接 `飞书` 形成工作流
10. `[未开始]` 学习 `Remotion` 到能够实际使用
11. `[未开始]` 学习 `macOS` 系统原生的好用软件
12. `[未开始]` 学习并实践使用一些针对 `PPT` 能力的 skill
13. `[未开始]` 探索 `Codex` 的锁屏操控功能
14. `[已完成]` 学习使用 `codex-chrome`，并探索这个功能是否只支持 `Chrome`、是否适配 `Edge`
15. `[未开始]` 将自己的 `Mac` 和 `Windows` 文件夹共享
16. `[未开始]` 学习怎么通过 `Mac` 调用 `Windows` 的算力
17. `[未开始]` 学习了解 `Codex` 跨账号合并信息
18. `[已完成]` 学习 `Codex` 链接手机端使用
19. `[未开始]` 探索 `appshot` 的能力边界
20. `[已完成]` 了解 `/goal` 模式
21. `[未开始]` 了解 `openclaw`
22. `[未开始]` 了解 `image2`

## 分组整理

### A. 基础能力

1. 学习 `Git` 和 `GitHub` 的完整流程
2. 学习 `Issue` 的使用方法
3. 学习 `Codex` 的浏览器控制和屏幕控制
4. 学习了解 `Codex` 跨账号合并信息
5. 学习 `Codex` 链接手机端使用
6. 探索 `Codex` 的锁屏操控功能
7. 了解 `/goal` 模式
8. 学习使用 `codex-chrome`，并探索这个功能是否只支持 `Chrome`、是否适配 `Edge`

### B. 能力增强

1. 学习 `skill: frontend-design`
2. 学习 `Humanizer`
3. 学习其他好用的 skill
4. 学习 `macOS` 系统原生的好用软件
5. 学习 `Remotion` 到能够实际使用
6. 学习并实践使用一些针对 `PPT` 能力的 skill
7. 探索 `appshot` 的能力边界
8. 了解 `openclaw`
9. 了解 `image2`

### C. 项目实践

1. 做一个自己的桌面待办清单应用
2. 根据现有的 `Excel` 记账表格做一个记账系统
3. 学习怎么通过 `Codex` 链接 `飞书` 形成工作流
4. 将自己的 `Mac` 和 `Windows` 文件夹共享
5. 学习怎么通过 `Mac` 调用 `Windows` 的算力

### D. 知识沉淀

1. 整理自己的知识并上传到知识库（当前已了解 `wiki` 知识库）
2. 整理并保存 `Codex` 和 `Claude Code` 的安装流程、经验和踩坑记录

## 已完成结果记录

耗时显示规则：
- 小于 `1` 小时时，显示多少 `min`
- 大于等于 `1` 小时且小于 `1` 天时，只显示多少 `h`
- 大于等于 `1` 天时，显示多少 `d` 多少 `h`

### 1. 学习 `Codex` 链接手机端使用

完成时间：2026-05-26，耗时：10h

结论：
- 手机端 `Codex` 更适合作为轻量远程接力入口，不适合重度长上下文开发
- 更适合做查看进度、回复澄清、审批动作、看简短结果
- 不适合长线程阅读、复杂代码审查、深度接管桌面端任务

### 2. 了解 `/goal` 模式

完成时间：2026-05-26，耗时：30min

结论：
- `/goal` 对应官方公开能力 `Goal mode`
- 核心用法是先定义 `outcome`、`success criteria`、`constraints`，再让 `Codex` 围绕目标持续推进
- 它更适合长任务、多步骤任务、需要阶段性自主判断的任务
- 不适合特别短的单次提问，也不适合你已经想亲自指定每一步的场景

可复用模板：

```text
/goal
目标（Outcome）：
- 我希望最终达成什么结果

成功标准（Success Criteria）：
- 必须满足的 3 到 5 条验收条件

范围（Scope）：
- 这次允许你处理哪些内容
- 明确不处理哪些内容

约束（Constraints）：
- 技术栈、文件范围、时间预算、风险边界、是否允许改动结构

工作方式（Working Style）：
- 先分析再执行，还是直接执行
- 遇到不确定点时先停下确认，还是先按最合理假设推进
- 是否需要在关键节点给阶段总结

交付物（Deliverables）：
- 我希望你最后输出什么
- 比如：代码修改、验证结果、风险说明、后续建议

补充上下文（Context）：
- 当前项目背景、已有问题、参考文件、参考链接、已有决定
```

推荐写法示例：

```text
/goal
目标（Outcome）：
- 做出一个可运行的桌面待办清单应用 MVP

成功标准（Success Criteria）：
- 支持新增待办
- 支持标记完成
- 支持删除待办
- 能在本地成功运行
- 界面简洁可用

范围（Scope）：
- 本次先完成最小可用版本
- 先不做账号系统、云同步、复杂提醒

约束（Constraints）：
- 优先复用现有技术栈
- 不做大范围重构
- 改动时尽量保持结构清晰

工作方式（Working Style）：
- 先快速分析项目结构，再直接实施
- 遇到影响方向的关键选择时先停下确认
- 完成后给出验证结果和下一步建议

交付物（Deliverables）：
- 可运行代码
- 简短变更说明
- 验证结果
- 风险和后续建议

补充上下文（Context）：
- 这是我自己的练手项目，目标是先跑通，再逐步增强
```

### 3. 学习 `Issue` 的使用方法

完成时间：2026-05-27，耗时：1h

结论：
- `Issue` 在软件协作里更适合理解成“被提出来并需要追踪处理的一件事项”
- 它不是只表示 `bug`，也可以承载 `feature request`、`task`、`question`、`feedback`
- 相比 `problem` 和 `task`，`Issue` 这个词更宽，更适合做协作容器
- 在 `GitHub` 里，一个好 `Issue` 通常至少要有标题、背景、问题或目标、验收标准、补充信息

### 4. 学习使用 `codex-chrome`，并探索这个功能是否只支持 `Chrome`、是否适配 `Edge`

完成时间：2026-05-29，耗时：2h

结论：
- `codex-chrome` 的核心价值是让 `Codex` 接入你真实的 `Chrome` 登录态和已有标签页
- 官方公开支持目标是 `Chrome`，当前没有看到官方公开声明支持 `Edge`
- 更适合拿来做登录态页面查看、轻量整理、网页内只读操作与低风险辅助操作
- 已实际验证可以接管 `Chrome` 会话并读取登录后的 `GitHub` 页面内容

## 追加规则

- 后续有新任务，直接追加到任务板中，并重新按优先级排序
- 默认新增任务状态为 `未开始`
- 开始推进某项时，更新为 `进行中`
- 完成后，更新为 `已完成`
- 暂时不做但又不想删掉的，更新为 `暂缓`
