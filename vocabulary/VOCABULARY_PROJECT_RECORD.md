# 专业词库项目记录

最后更新：2026-05-27

## 1. 关键文件

- `/Users/laiming/Documents/Workspace/Codex/vocabulary.html`
- `/Users/laiming/Documents/Workspace/Codex/vocabulary.csv`
- `/Users/laiming/.codex/AGENTS.md`

## 2. 当前项目定位

这是一个本地单文件词库页面，主要用于：

- 积累 AI / 编程 / 协作场景中的英文词汇
- 在词库页查看、筛选、标记学习状态
- 在复习页按卡片式流程做主动回忆

## 3. 当前已完成功能

### 词库页

- 英文、中文、通俗解释/专业解释、例句、常见场景、学习状态、类型、词性
- 英文词支持喇叭发音
- 例句支持：
  - 点击展开中文翻译
  - 句尾喇叭朗读英文例句
- 搜索、按类型筛选、按学习状态筛选
- 顶部统计卡：
  - 总词库
  - 已学会
  - 未学会
  - 待复习

### 学习状态

- 点击切换 已学会 / 未学会
- 排序规则已做过多轮调整：
  - 未学会区：新加入或重新打回未学会的词优先靠前
  - 已学会区：最近学会的词优先靠前

### 复习页

- 模式切换：词库 / 复习
- 今日复习概览
- 卡片式复习流程：
  - 先看英文、音标、发音
  - 点击“显示答案”
  - 再选：
    - 没记住
    - 有点印象
    - 记住了

## 4. 当前复习时间机制

### 时间边界

- 短间隔仍按分钟算
- “天”的切换边界使用北京时间 `06:00`

### 规则

- `没记住`
  - 本轮稍后回炉
  - `10 分钟后` 再次到期
- `有点印象`
  - 下一个学习日（北京时间 06:00 边界）
- `记住了`
  - 第 1 次连续记住：第 `3` 个学习日
  - 第 2 次连续记住：第 `7` 个学习日
  - 第 3 次及以上：第 `14` 个学习日

### 相关状态字段

- `status`
- `learnedAt`
- `unlearnedAt`
- `reviewLevel`
- `lastReviewedAt`
- `nextReviewAt`
- `lastReviewResult`
- `reviewCount`
- `correctCount`
- `wrongCount`
- `fuzzyCount`
- `streak`

## 5. 当前朗读机制

### 单词朗读

- 速度已调稳，当前用户反馈可接受

### 例句朗读

- 单独速度：`0.7`
- 已处理首次打开页面时“沙哑声音”的问题
- 已做语音预热与偏好语音选择

## 6. 当前词库规模

- 当前总词条：`104`

说明：
- 词库数据同时存在于 `vocabulary.csv` 与 `vocabulary.html` 内嵌数据中
- 每次新增词时，需要同步这两处

## 7. 已确认的跨浏览器状态问题

### 现象

- 在 Codex 内置浏览器里标记的 已学会 / 未学会 状态
- 与用户自己浏览器里打开同一个 `vocabulary.html`
- 可能不一致

### 已查明原因

当前状态保存分三层：

1. `URL hash` 中的 `#state=...`
2. `localStorage`
3. `IndexedDB`

但其中：

- `localStorage` 是浏览器实例自己的
- `IndexedDB` 也是浏览器实例自己的
- Codex 内置浏览器和用户自己的浏览器，不共享这两层本地存储

所以：

- 在 Codex 里改的状态，会存在 Codex 自己的 `localStorage / IndexedDB`
- 在你自己的浏览器里改的状态，会存在你自己浏览器的 `localStorage / IndexedDB`
- 两边天然是两套独立数据

### URL hash 的作用和限制

理论上，如果使用**完全相同**的 URL（包括 `#state=...`）打开，两边可以共享一部分状态。

但实际使用中：

- 用户通常是直接双击打开 `vocabulary.html`
- 或在不同浏览器里重新打开裸文件路径
- 这时不会自然带上另一边的完整 hash 状态

所以最终表现出来就是：

- “同一个 HTML，在不同浏览器里状态不一致”

### 复习模块会不会受影响

会。

因为复习模块和词库页共用的是同一套状态缓存：

- `learnedStateCache`
- 同一组 `localStorage / IndexedDB / hash`

所以只要学习状态不同步：

- 待复习数量
- 今日复习内容
- 复习进度字段

也都会跟着不同步。

## 8. 这不是代码 bug 的部分

这更接近“架构层面的天然隔离”，不是简单的前端小 bug。

只要页面还是：

- 本地 `file://` 打开
- 纯前端
- 没有共享后端或共享文件写入能力

那不同浏览器之间不共享状态，本身就是预期行为。

## 9. 如果后面要真正解决跨浏览器同步

可选方向：

### 方案 A：继续维持现状

- 只固定在一个浏览器里使用
- 成本最低
- 但跨浏览器不一致问题继续存在

### 方案 B：导出 / 导入状态

- 增加“导出学习状态 JSON”
- 增加“导入学习状态 JSON”
- 适合偶尔同步

### 方案 C：增加一个本地小后端

- 由本地服务统一读写状态文件
- 不同浏览器都访问同一个本地状态源
- 这是最稳的真正解决方案
- 但实现复杂度明显更高

## 10. 用户偏好（与本项目高度相关）

- 默认中文回复
- 专业词学习相关对话里：
  - 用户发陌生专业词
  - 询问英文单词
  - 发包含英文专业内容的截图
  - 默认先翻译解释，再自动加入本地专业词库
- 每次有新词被自动加入词库时：
  - 结束时用自然、不生硬的方式告知用户已完成入库

相关规则已写入：

- `/Users/laiming/.codex/AGENTS.md`

## 11. 最近一次重要提交

- commit: `32b339f`
- message: `Update vocabulary app and review workflow`

## 12. 后续建议优先级

### 高优先级

- 决定是否要解决“跨浏览器状态不一致”
- 如果要解决，先在“导入导出”与“本地后端”之间选方向

### 中优先级

- 持续扩词
- 继续补 AI / 编程 / 协作 / 基础英语词

### 低优先级

- 进一步美化页面
- 更细的复习统计与回顾视图
